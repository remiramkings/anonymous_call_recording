name: anonymous_call_recorder
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_foreground_task: ^6.1.2
  flutter_local_notifications: ^16.1.0
  path_provider: ^2.1.1
  permission_handler: ^11.0.1
  phone_state_background: ^0.0.4
  record: ^5.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
